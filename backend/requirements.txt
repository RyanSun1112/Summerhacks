# Flask sensor-logger sidecar ONLY (optional local research).
# Not used by the Node deploy (server.js serves /api/snapshots when SQLite is on).
# Do not treat this file as the web service runtime — see render.yaml / package.json.

Flask==3.1.3
Flask-Cors==6.0.5
